name: Azure Test Build and Deploy

on:
  # Old push-triggered Azure test deploy is intentionally disabled.
  # Final service delivery should use:
  # PR to main -> approved review + build label -> ACR image -> Helm values -> ArgoCD sync.
  # Keep this workflow manual-only for bootstrap/break-glass testing.
  # push:
  #   branches:
  #     - refactor/saas-foundation-inventory
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Optional immutable image tag. Defaults to azure-test-<run-number>-<sha>."
        required: false
        type: string
      deploy:
        description: "Deploy to AKS after pushing the image."
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  id-token: write

env:
  ACR_NAME: ${{ vars.AZURE_TEST_ACR_NAME }}
  IMAGE_REPOSITORY: opslora-payment-service
  HELM_REPO: KUBE-POD404/opslora-helm-charts
  HELM_REF: test/opslora-helm-test
  CHART_PATH: opslora-helm/apps/payment
  RELEASE_NAME: payment
  NAMESPACE: opslora-app-ns
  MIGRATE_JOB: payment-service-migrate
  AZURE_KEY_VAULT_ENABLED: "true"

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment: azure-test

    steps:
      - name: Checkout service
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_TEST_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_TEST_SUBSCRIPTION_ID }}

      - name: Resolve ACR image metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${ACR_NAME}" ]; then
            echo "Missing GitHub environment variable AZURE_TEST_ACR_NAME." >&2
            exit 1
          fi
          login_server="$(az acr show --name "${ACR_NAME}" --query loginServer -o tsv)"
          if [ -z "$login_server" ]; then
            echo "Could not resolve ACR login server for ${ACR_NAME}." >&2
            exit 1
          fi
          if [ -n "${{ github.event.inputs.image_tag || '' }}" ]; then
            tag="${{ github.event.inputs.image_tag }}"
          else
            tag="azure-test-${{ github.run_number }}-${GITHUB_SHA::7}"
          fi
          image="$login_server/${IMAGE_REPOSITORY}"
          echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo "login_server=${login_server}" >> "$GITHUB_OUTPUT"
          echo "image=${image}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Azure Container Registry
        shell: bash
        run: az acr login --name "${ACR_NAME}"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}
            ${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.short_sha }}

      - name: Check Helm repo token
        if: ${{ github.event.inputs.deploy != 'false' }}
        shell: bash
        env:
          HELM_REPO_TOKEN: ${{ secrets.HELM_REPO_TOKEN }}
        run: |
          if [ -z "${HELM_REPO_TOKEN}" ]; then
            echo "Missing HELM_REPO_TOKEN. Add a token with read/write access to KUBE-POD404/opslora-helm-charts." >&2
            exit 1
          fi

      - name: Checkout Helm charts
        if: ${{ github.event.inputs.deploy != 'false' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ env.HELM_REPO }}
          ref: ${{ env.HELM_REF }}
          path: helm-repo
          persist-credentials: false

      - name: Install yq
        if: ${{ github.event.inputs.deploy != 'false' }}
        shell: bash
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Update Helm values
        if: ${{ github.event.inputs.deploy != 'false' }}
        working-directory: helm-repo
        env:
          HELM_REPO_TOKEN: ${{ secrets.HELM_REPO_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          values_file="${CHART_PATH}/values-azure-test.yaml"
          yq -i '.image.repository = "${{ steps.meta.outputs.image }}"' "$values_file"
          yq -i '.image.tag = "${{ steps.meta.outputs.tag }}"' "$values_file"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$values_file"
          if git diff --cached --quiet; then
            echo "No Helm values changes to commit"
          else
            git commit -m "ci(payment): update azure test ACR image to ${{ steps.meta.outputs.tag }}"
            for attempt in 1 2 3; do
              if git push "https://x-access-token:${HELM_REPO_TOKEN}@github.com/${HELM_REPO}.git" HEAD:${HELM_REF}; then
                exit 0
              fi
              echo "Helm branch moved, rebasing before retry ${attempt}..."
              git pull --rebase origin "${HELM_REF}"
              yq -i '.image.repository = "${{ steps.meta.outputs.image }}"' "$values_file"
              yq -i '.image.tag = "${{ steps.meta.outputs.tag }}"' "$values_file"
              git add "$values_file"
              if ! git diff --cached --quiet; then
                git commit --amend --no-edit
              fi
            done
            git push "https://x-access-token:${HELM_REPO_TOKEN}@github.com/${HELM_REPO}.git" HEAD:${HELM_REF}
          fi

      - name: Set AKS context
        if: ${{ github.event.inputs.deploy != 'false' }}
        uses: azure/aks-set-context@v4
        with:
          resource-group: rg-opslora-test-aks-cin
          cluster-name: aks-opslora-test-cin-001

      - name: Set up Helm
        if: ${{ github.event.inputs.deploy != 'false' }}
        uses: azure/setup-helm@v4

      - name: Deploy to AKS
        if: ${{ github.event.inputs.deploy != 'false' }}
        working-directory: helm-repo
        shell: bash
        run: |
          set -euo pipefail
          kubectl create namespace "${NAMESPACE}" --dry-run=client -o yaml | kubectl apply -f -
          if [ -n "${MIGRATE_JOB}" ]; then
            kubectl delete job "${MIGRATE_JOB}" -n "${NAMESPACE}" --ignore-not-found
          fi
          helm upgrade --install "${RELEASE_NAME}" "${CHART_PATH}" \
            --namespace "${NAMESPACE}" \
            -f "${CHART_PATH}/values.yaml" \
            -f "${CHART_PATH}/values-azure-test.yaml" \
            --set image.repository="${{ steps.meta.outputs.image }}" \
            --set image.tag="${{ steps.meta.outputs.tag }}" \
            --set azureKeyVault.enabled="${AZURE_KEY_VAULT_ENABLED}" \
            --wait --timeout 5m
